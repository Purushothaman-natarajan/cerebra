# HTTP — redirects to HTTPS in production
server {
    listen 80;
    server_name _;

    # Redirect to HTTPS when SSL certs are configured
    # Uncomment the return directive and configure the HTTPS server below:
    # return 301 https://$host$request_uri;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;

    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /ws/ {
        proxy_pass http://backend:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, must-revalidate";
    }
}

# HTTPS — enable when SSL certificates are available
#server {
#    listen 443 ssl;
#    server_name cerebra.example.com;
#
#    ssl_certificate /etc/ssl/certs/cerebra.crt;
#    ssl_certificate_key /etc/ssl/private/cerebra.key;
#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_ciphers HIGH:!aNULL:!MD5;
#
#    root /usr/share/nginx/html;
#    index index.html;
#
#    gzip on;
#    gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript image/svg+xml;
#
#    location /api/ {
#        proxy_pass http://backend:8000/;
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#    }
#
#    location /ws/ {
#        proxy_pass http://backend:8000;
#        proxy_http_version 1.1;
#        proxy_set_header Upgrade $http_upgrade;
#        proxy_set_header Connection "upgrade";
#        proxy_set_header Host $host;
#    }
#
#    location / {
#        try_files $uri $uri/ /index.html;
#        add_header Cache-Control "no-cache, must-revalidate";
#    }
#}
